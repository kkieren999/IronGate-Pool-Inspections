{
  "name": "irongate-calendar-functions",
  "description": "Google Calendar event creation for paid IronGate bookings",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "deploy": "firebase deploy --only functions:calendar",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.2.5",
    "googleapis": "^165.0.0"
  }
}
